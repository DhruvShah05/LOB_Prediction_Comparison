# ──────────────────────────────────────────────────────────────────────────────
# Core dependencies — pinned for reproducibility (build.md §4)
# ──────────────────────────────────────────────────────────────────────────────

# Data handling
pandas==2.2.1
numpy==1.26.4
pyarrow==15.0.0          # required for parquet I/O (crypto dataset)
huggingface_hub==1.29.0  # pinned for reproducibility; required for downloading datasets from Hugging Face buckets

# Tree models
scikit-learn==1.4.1.post1
xgboost==2.0.3

# Neural models
# NOTE: The version below installs CPU-only PyTorch.
# For GPU support, install BEFORE running pip install -r requirements.txt:
#   Lightning AI / Colab / CUDA machine:
#     pip install torch==2.2.1 --index-url https://download.pytorch.org/whl/cu121
#   Apple Silicon (MPS):
#     pip install torch==2.2.1          (default wheel includes MPS support)
torch==2.2.1

# Config
pyyaml==6.0.1

# Hyperparameter tuning
optuna==3.5.0

# Stats / significance testing
scipy==1.12.0

# Plotting (reporting scripts only, not training code)
matplotlib==3.8.3

# Progress bars
tqdm==4.66.2

# Testing
pytest==8.0.2
